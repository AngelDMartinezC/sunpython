[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "sunpython"
version = "0.1.0"
dependencies = [
    "sunpy",
    "polarTransform",
    "reproject",
    "joblib",
    "astropy"
]
